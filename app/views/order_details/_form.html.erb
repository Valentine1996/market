<%= form_for(@order_detail) do |f| %>
  <% if @order_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_detail.errors.count, "error") %> prohibited this order_detail from being saved:</h2>

      <ul>
      <% @order_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.collection_select(:product_id, @products, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.collection_select(:order_id, @orders, :id, :id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
